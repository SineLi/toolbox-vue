## 快速上手

点击上传区域或拖拽 `.spd`文件到页面，点击下载后页面自动将`SPD光谱文件`转换为`CSV文件`并下载。

### 设置参数
**压缩下载**：将多选的文件以单个压缩包形式下载，打包过程在浏览器上进行，可以避免同时下载多个文`

**自动下载**：上传的文件将会立即转换格式并下载

*注意：压缩下载和自动下载无法同时开启*

## 注意事项
该工具目前只能识别`UVWIN 5.0`保存的`SPD格式光谱文件`，其他格式或文件类型无法正确处理